# ENVIRONMENT-LOCK-V1 measured constraints
#
# 這不是安裝需求檔，是一份「量測結果」。它記錄下面這個 lock record 實際觀測到
# 的 exact versions，供 re-execution 時重建同一個環境並以
#   python environment_lock.py verify <record>
# 驗證。requirements.txt 的 >= floors 描述的是一個無上界的環境集合，不能用來
# 重驗任何數值結果；本檔描述的是一個環境。
#
# record        : backend/environment_locks/lock-2026-09-08-remote-dev-container.json
# locked_sha256 : sha256:d350a110ca9a1f3a5c063c2d12996ef63a74ceba1a324b5f2098eab031fa3d7c
# captured_at   : 2026-09-08T07:47:23Z
# architecture  : Linux/x86_64
# interpreter   : CPython 3.11.15
# completeness  : FULL_LOCK
# threading     : AMBIENT_THREADING_NOT_PINNED
#
# [BLOCKER] Version pins 不足以保證數值可重現：同一個版本可以連到不同的 BLAS 或用
# 不同的 SIMD kernel。必須同時比對 record 內的 numeric/plant/learning fingerprints。
# 詳見 docs/ENVIRONMENT_LOCK_SPEC.md。

cloudpickle==3.1.2
fastapi==0.141.1
gymnasium==1.3.0
httpx==0.28.1
mujoco==3.12.0
numpy==2.4.6
orjson==3.12.0
pydantic==2.13.5
pytest==8.4.2
stable-baselines3==2.9.0
torch==2.14.0
uvicorn==0.52.4
